# yaml-language-server: $schema=https://aka.ms/teams-toolkit/v1.7/yaml.schema.json
# QQIA Agent - Teams Toolkit config (Codespace deployment - no Azure needed)

version: v1.7

environmentFolderPath: ./env

provision:
  # Creates a new AAD app to authenticate the bot
  - uses: botAadApp/create
    with:
      name: QQIA-Agent-${{TEAMSFX_ENV}}
    writeToEnvironmentFile:
      botId: BOT_ID
      botPassword: SECRET_BOT_PASSWORD

  # Register bot with Bot Framework (points to Codespace URL)
  - uses: botFramework/create
    with:
      botId: ${{BOT_ID}}
      name: QQIA-Agent
      messagingEndpoint: https://${{BOT_DOMAIN}}/api/messages
      description: "FY27 Mint Rollover Tracker"
      channels:
        - name: msteams

  # Creates a Teams app
  - uses: teamsApp/create
    with:
      name: QQIA Agent-${{TEAMSFX_ENV}}
    writeToEnvironmentFile:
      teamsAppId: TEAMS_APP_ID

  # Validate the manifest
  - uses: teamsApp/validateManifest
    with:
      manifestPath: ./appPackage/manifest.json

  # Build Teams app package
  - uses: teamsApp/zipAppPackage
    with:
      manifestPath: ./appPackage/manifest.json
      outputZipPath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
      outputFolder: ./appPackage/build

  # Apply the Teams app manifest
  - uses: teamsApp/update
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
projectId: fab0c121-96a8-478a-805c-c26450356686
